export class TrApplication {
    constructor(appId, talentId, jobId, statusId, rating, comment, talent, job, status) {
      this.appId = appId; 
      this.talentId = talentId;
      this.jobId = jobId; 
      this.statusId = statusId;
      this.rating = rating; 
      this.comment = comment; 
      this.talent = talent; 
      this.job = job;
      this.status = status;
    }
  }